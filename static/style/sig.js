/* sig.js */
/* unused right now */

/* 
The parameters must always be:
useracc = User type object
bankacc = Bank type object
adming = admin integer level
*/

function sig(user_id) {
    let useracc={{ useracc | tojson}};
    let bankacc={{ useracc | tojson}};
    let adming={{adming}};
    
    var username=`${useracc[1]}`;
};
